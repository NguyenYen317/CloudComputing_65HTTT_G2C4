// Firebase Authentication is used by the Flutter client. The backend currently
// maps the Google login payload sent by the app to an application user.
module.exports = {
  handledByClient: true,
};
