import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  static const primary = Color(0xffff5a1f);
  static const background = Color(0xfffaf7f4);
  static const surface = Colors.white;
  static const border = Colors.black12;
  static const textMuted = Color(0xff6f625d);
}
